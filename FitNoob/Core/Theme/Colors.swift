import SwiftUI

struct AppColors {
    static let background = Color.black
    static let surface = Color(white: 0.08)
    static let surfaceLight = Color(white: 0.12)
    static let text = Color.white
    static let textSecondary = Color.gray
    static let accent = Color.green
}
